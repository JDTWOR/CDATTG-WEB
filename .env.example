# ===========================================
# CDATTG - Docker / Producción
# Copiar a .env y ajustar valores.
# ===========================================

# Base de datos PostgreSQL
DB_USER=cdattg
DB_PASSWORD=cambiar-en-produccion
DB_NAME=cdattg_web
DB_PORT=5432
DB_SSLMODE=disable
DB_TIMEZONE=America/Bogota

# Backend (JWT)
JWT_SECRET=generar-clave-secreta-minimo-256-bits-para-produccion
JWT_EXPIRATION_HOURS=24

# CORS: orígenes permitidos (front en HTTPS)
CORS_ALLOWED_ORIGINS=https://academica.jhonrojas.online

# URL de la API usada por el frontend en tiempo de build
VITE_API_BASE_URL=https://apiacademica.jhonrojas.online/api
